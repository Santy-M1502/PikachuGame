import { RespuestaEstado } from './respuesta-estado';

describe('RespuestaEstado', () => {
  it('should create an instance', () => {
    const directive = new RespuestaEstado();
    expect(directive).toBeTruthy();
  });
});
